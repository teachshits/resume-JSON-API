Built in Ruby v 1.9.3 running on OSX 10.6.8

Using 3 gems, Sinatra and json plus rack-test for testing.

Test suite is in test.rb.

Available at http://evening-beach-7507.herokuapp.com/

API is accessed via ./api/ and returns JSON
./api/resume/contact 
	returns a contact information object
./api/resume/education
	returns an array of education objects
./api/resume/jobs
	returns an array of employer objects
./api/resume/skills/
	returns an array of skill strings
./api/resume
	returns an object containing all of the above calls
	
. or ./index.html
	An empty resume page populated by a getJSON call to ./api/resume